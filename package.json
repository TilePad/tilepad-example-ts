{
  "name": "tilepad-plugin",
  "version": "0.0.1",
  "description": "Tilepad + Typescript counter example plugin",
  "type": "module",
  "scripts": {
    "build": "tsc && rollup --config",
    "bundle": "npm run build && tilepad bundle --name plugin",
    "prepublishOnly": "npm run build"
  },
  "author": "Jacobtread",
  "license": "MIT",
  "dependencies": {
    "@tilepad/cli": "^0.5.2",
    "@tilepad/sdk": "0.0.2"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.1.2",
    "@types/node": "^24.1.0",
    "prettier": "^3.4.2",
    "rollup": "^4.29.1",
    "tslib": "^2.8.1",
    "typescript": "^5.7.2"
  }
}
